

<h2>User info</h2>
  <% if user.image.attached? %>
    <%= image_tag user.image %>
  <% else %>
    <%= image_tag 'no_image' %>
  <% end %>
  <table class="table">
    <tbody>
      <tr></tr>
      <tr>
        <th>name</th>
        <th><%= user.name %></th>
      </tr>
     <tr>
       <th>introduction</th>
       <th></th>
     </tr>
    </tbody>
  </table>

  <div class="row">
  <%= link_to "introduction",edit_user_path(user) , class:"btn btn-outline-secondary btn-block" %>
  </div>

